When done with a change, run the web server and see with the playwright skill that there are no javascript errors in the console. If there are, fix them before proceeding.

If the Vite dev server is already running and Codex needs it to reload config or restart from inside the sandbox, use `POST /dev/reload` against the running dev server instead of killing the process directly.

Skills are in .claude/skills
